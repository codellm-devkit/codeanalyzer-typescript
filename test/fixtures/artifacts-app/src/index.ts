import express from "express";
import leftPad from "left-pad";
import type { SomeType } from "typed-only-pkg";
import * as fs from "node:fs";
export function main(): void { void express; void leftPad; void fs; const t: SomeType | null = null; void t; }
